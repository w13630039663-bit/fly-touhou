# PLAN_520 评审报告

> 审查人：喵酱 ｜ 日期：2026-09-15 ｜ 方法：**不复述计划文字，逐条把计划引用的每个数字回源到代码/数据独立复算**
> 复算脚本：`experiments/_review_share.mjs`、`_review_share2.mjs`、`_review_struct2.mjs`、`_review_drive.mjs`、`_review_log.py`；原输出 `_review_*_out.txt`

---

## 一句话结论

**方向正确、结构专业，可以执行，但有 4 处必须先修正——其中 2 处会直接影响实验结论的有效性。**

PLAN_520 本质是把 `HANDOVER_DOCUMENT.md §六 待办#3` 的「验证可逆性」正确工程化了：诊断（DN 读出稀释）经独立复算**成立**；A（读出约定）/ C（建图约定）两条路线分别精准命中两个嫌疑点；P0「先装仪表测 Δ₀ 再治」的顺序尤其专业。扣分项集中在**数字口径未写明**与**两个被忽略的结构事实**。

---

## 一、前提复核：计划引用的数字，逐条回源

| 计划引用 | 我的独立复算 | 判定 |
|---|---|---|
| 600 vs 80 回归 −20.0%，Δ=+309f，p=0.0065，17/20 | `scale_why.mjs` 实测：差 **309.3f**，**p=0.0065**，**17/20** | ✅ 精确一致 |
| 活跃率 51.7%→8.4% | 实测 **51.7% → 8.4%** | ✅ 精确一致 |
| dnRms ÷3.3（基线 0.42） | 实测 **1.37 → 0.422**（÷3.25） | ✅ 一致 |
| DN 入联系 635→2788（×4.4，出自 HANDOVER §六） | 实测 **634.8 → 2787.5** | ✅ 精确命中 |
| **直连份额 23.4%→8.5%** | 定义未写明。最接近口径＝「感觉细胞直连边占 DN 入边比例」：**21.7% → 8.3%**（权重中位口径 24.5%→4.5%） | ⚠️ 方向成立，**定义缺失、精确值不复现** |
| **对照 1080/1097/1130 平台 > bio 1020** | 这是**训练日志"历史最佳"峰值**：ctrl = **1129.6 / 1097.2 / 1079.7**，bio = **1041.1**（1020.2 是 bio 第 25–45 代的中期值）。**独立测试集**则是 bio **1235.6** vs ctrl **1567.4 / 877.9 / 880.3** | ⚠️ **训练平台 ≠ 测试集**；测试集为 1 胜 2 负，bio 均值反高 **+11.5%** |

> 结论：4 项机制数字全部成立（其中 2 项精确命中）——**诊断可信**。但 premise 第 3 句（"对照反而优于 bio"）用的是训练平台值，而独立测试上是"有赢有输"。这一点必须改写，否则会被自己的数据打脸。

---

## 二、机制诊断：成立，附完整实测链路

| 我实测的结构事实 | 数值 |
|---|---|
| `graph600.json` sha256 三方一致（文件 / manifest600 / checkpoint600） | `d3b38436…a1144` ✓ |
| v1-80 是否为 v600 的子集 | **80/80 全在**，新细胞 = **恰好 520** ✓ |
| 520 新细胞角色 | **24 个输入 + 496 个中间**（不是纯中间！） |
| DN 入边来源 | 来自 v1 细胞 **244 条** / 来自 520 新细胞 **833 条**（新占 **77.3%**） |
| per-post 归一化分母（`connectome.js:79`） | **632 → 2737（×4.3）** |

稀释机制 = **数学必然**（`weight = contacts·sign / totals[post]`，`totals` 随规模膨胀而分母变大），不是偶然噪声。**诊断成立，A/C 值得做。**

---

## 三、逐项评审

### P0 装仪表 — ★★★★☆（最该先做）
- **P0.1 ✓** 名单可由 body id 集合差确定性推导，我实测 = 520 无误。建议名单落下时同时写 `sha` 进 manifest，保持项目一贯的哈希链纪律。
- **P0.2 ⚠️ 有实质缺口**：v600 的 56 个输入细胞 = **32 原 + 24 新**。若按"静音全部 520 新细胞"，会**顺带掐掉 24/56（43%）的感觉输入细胞**——Δ₀ 就同时混入了"去掉新中间层计算"与"去掉 24 路感觉输入"两个效应。**建议拆两个口径**：`Δ₀(496 新中间)` 与 `Δ₀(520 全部)`，先报前者为主。
- **P0.3 ✓** v1 图无名单 ⇒ 静音集为空 ⇒ 行为不变，这个护栏极其廉价且正确。

### A 修读出约定 — ★★★★☆
- **A.1 ⚠️ 必须写明"两段归一化如何合并"**。现文字"原始 v1 边分母冻结 + 新边单独除以新总入"有两种读法：
  - **加性**：v1 段 |w| 合计 1 ＋ 新段 |w| 合计 1 ＝ 合计 2 → DN 驱动整体放大，dnRms 才可能升到 ≥1.0；
  - **共享**：两段合计仍为 1 → dnRms 有界，判据"≥1.0"基本达不到。
  判据 `dnRms ≥ 1.0` **只在加性口径下自洽**，请把这句定义写进 A.1。
- **A.2 判据主次要调**：把"平台从 1020f 上移 ≥150f"降为参考，**主判据用"20 种子配对显著的 Δ"**。单一 plateau 受 CEM 单次随机主导（对照三组就能从 878 飘到 1567），单值阈值容易误判。

### C 改图约定 — ★★★★☆
- **C.1 ⚠️ "v1 原边全保留"不是默认就有**：v1 manifest 是「all measured internal directed edges」，v600 是「>=3 contacts」。实测 **v1 的 1296 条边在 v600 里只剩 846 条**（450 条 <3 接触的低权重原边被阈值裁掉了）。要做到"全保留"，**必须显式放回这 450 条**，否则 C 的对照不干净。
- **C.1 ⚠️ 有一个必须写明的后果**：剔除 833 条「新→DN」边、放回 358 条「v1→DN」边后，DN 的入边集合与 per-post 分母**完全回到 v1** ⇒ **DN 的读出权重会与 v1 逐位相同**。也就是说 **C 实际等价于「v1 读出 + 520 细胞对 v1 上游的调制」**。这是很好的设计，但如果不写明，A vs C 的对决结论会被误读（"C 赢"不代表"新细胞有用"，而是"别让新细胞碰读出"）。

### B reservoir 把手 — ★★★☆☆
- **B.1 ⚠️ 会撞硬契约**：`test_integration.js:208` 有 `if (PARAMETERS !== 320) throw`。320→322 必须同步改：`policy.js`(PARAMETERS)、`train.js`、`tools/cem_worker.mjs`、checkpoint 格式、`test_integration.js:208`、UI methods 文案。计划只说了"workers/checkpoint/UI"，**漏了测试断言这条**。~1h 估计偏紧。
- "增益=1 时逐帧位级一致"作为回归门 —— ✅ 完全正确，保留。

### D 预算放大 — ★★★☆☆
- **✓** "v1 同预算对称放大"的自省非常对（避免重犯"预算不对称"的错）。
- **✓** 触发条件与方向判据（只有 600x 超 v1x 才敢说"规模带来能力"）都正确。
- **机时待校准**：按本次 600 训练 ~2233s / 6400 次候选评估推算，300代×128 约 **4–5h/拓扑**，而非计划的 6h；建议跑前用 1 代实测标定。

### E 收尾 — ✓
哈希链 + 测试绿 + 待办销账，符合项目纪律。§六 待办 #2（600 LOD 人眼验收）建议顺带做掉。

---

## 四、计划遗漏的三点（建议补进去）

1. **重训不可复现**：`train.js:137` 的 CEM 采样用的是 **`Math.random()`（未播种）**，所谓"固定同参数同种子配方"只能固定**游戏种子/对照种子**，**权重搜索本身不可复现**。⇒ A/C/B/D 之间的一切对比**只能靠 20 种子配对检验**，绝不能靠单次 plateau 数值。**强烈建议给 CEM 播种**（改动 <10 行，换来全链路可复现，性价比极高），并把它插进 P0。
2. **Δ₀ 的输入细胞混淆**（见 P0.2）。
3. **"对照优于 bio"的表述**（见前提表）：建议改为——
   > 训练平台上三组度保持对照均不低于 bio（1041 vs 1079/1097/1130），说明**预算不是唯一瓶颈**；但独立测试集为 1 胜 2 负、bio 均值反高 11.5%，故**不足以宣称"对照更优"**，只支持"这张子图的输出侧信号质量是可疑瓶颈"。

---

## 五、执行顺序建议

`P0（+给 CEM 播种）→ A / C 并行建图 → 同预算重训对决 → B（在最优拓扑上）→ D（条件触发）`

这个顺序本身就很对，我唯一想插队的只有「给 CEM 播种」——10 分钟的成本，换来后面所有实验的可复现性。

---

## 六、总评

**8 / 10。** 诊断扎实（4 项机制数字全部复现）、A/C 对决设计优雅（一次性判别"读出约定 vs 建图约定"谁是主犯）、护栏意识极强（v1 位级不动 / typed-vs-legacy Δ=0 / 方向一致性门 / No game outcomes used / 原始记录留存）。

扣分全在"口径"与"边界"：两个数字口径未写明（23.4/8.5、平台 vs 测试集）、P0.2 的输入细胞混淆、C.1 的"全保留"与"后果"未说明、B.1 漏了测试断言。**修正这 4 处即可放心执行。**
